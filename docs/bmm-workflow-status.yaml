# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-12-01"
project: "pickle"
project_type: "Pickleball Expense Split - Multi-device web app for fair cost splitting"
selected_track: "quick-flow"
field_type: "brownfield"
workflow_path: ".bmad/bmm/workflows/workflow-status/paths/quick-flow-brownfield.yaml"

workflow_status:
  # Prerequisite: Documentation (brownfield only)
  prerequisite:
    document-project: "optional"

  # Phase 0: Discovery (Optional)
  discovery:
    brainstorm-project: "optional"
    research: "skipped"

  # Phase 1: Planning
  planning:
    tech-spec: "docs/tech-spec.md"
    story-session-management-1: "required"
    story-session-management-2: "pending"
    story-session-management-3: "pending"
    story-session-management-4: "pending"
    story-session-management-5: "pending"

  # Phase 2: Implementation
  implementation:
    sprint-planning: "required"
    note: "After sprint-planning, track stories in sprint-status.yaml"
